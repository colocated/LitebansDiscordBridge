# paper-plugin.yml — used by Paper 1.19.4+ in preference to plugin.yml.
# Servers older than 1.19.4 fall back to plugin.yml automatically.
name: LiteBansDiscordBridge
version: '${project.version}'
main: dev.colocated.litebansdiscordbridge.bukkit.LiteBansDiscordBridgeBukkit
description: Sends LiteBans events to Discord webhooks
api-version: '1.19'
authors:
  - colocated
dependencies:
  server:
    litebans:
      load: BEFORE
      required: true
      join-classpath: true
